language: perl

install:
    - sudo apt-get update -qq
    - sudo apt-get install -qq --no-install-recommends debhelper devscripts equivs

script:
    - mk-build-deps -i -r
    - debuild --no-tgz-check -uc -us
